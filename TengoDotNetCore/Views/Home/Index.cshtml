@{
    ViewData["Title"] = "首页";
    var banners = ViewBag.Banners as List<Article> ?? new List<Article>();
    var goods = ViewBag.Goods as List<Goods> ?? new List<Goods>();
}
@section Header{
    <link href="~/lib/swiper/swiper.min.css" rel="stylesheet" />
}
<div class="container-fluid">
    <!--banner start-->
    <div class="swiper-container swiper-container swiper-container-horizontal banner">
        <div class="swiper-wrapper">
            @for (var i = 0; i < banners.Count; i++) {
                <div class="swiper-slide text-center">
                    <a href="@banners[i].LinkUrl" target="_blank">
                        <img src="@banners[i].CoverImg" style="" />
                    </a>
                </div>
            }
        </div>
        <div class="swiper-pagination"></div>
    </div>
    <!--banner end-->
</div>
<br />
<div class="container home-index">
    <ul class="clearfix index-goods">
        @foreach (var item in goods) {
            <li class="float-left" title="@item.Name @item.NameEn">
                <a href="/goods/detail?id=@item.ID">
                    <img src="@item.CoverImg" style="width:180px;height:180px;" />
                </a>
                <p class="text-singleline color-red">￥ @item.Price</p>
                <a href="/goods/detail?id=@item.ID">
                    <p class="goods-name">@item.Name&ensp;&ensp;@item.NameEn</p>
                </a>
            </li>
        }
    </ul>
</div>

@section Scripts{
    <script src="~/lib/swiper/swiper.min.js"></script>
    <script>
        /*轮播baner start*/
        var swiper1 = new Swiper('.swiper-container', {
            loop: true,
            autoplay: true,
            speed: 1000,
            pagination: {
                el: '.swiper-pagination',
                clickable: true,
            }
        });

        $(".swiper-pagination-bullet").hover(function () {
            $(this).click();
        }, function () {
            swiper1.autoplay.start();
        })
    </script>
}